import AccountClient from "@/components/partials/Account";

export const metadata = {
  title: "Account",
  description:
    "Manage your account details, order history, wishlists, and more on WOW, your one-stop e-commerce platform for all your shopping needs.",
};

const Account = () => {
  return <AccountClient />;
};

export default Account;
